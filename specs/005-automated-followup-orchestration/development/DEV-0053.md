---
id: DEV-0053
feature: FEAT-05
plan: DES-05
component: DES-05-CMP-009
component_type: flow_cloud
executor: agent
owner: null
build_skill: power-automate-flow
implementation_scope: repository_and_dataverse_solution
execution_host: local_interactive
authoring_target:
  authentication_mode: interactive_user
  connection_alias: dataverse-authoring
  environment: authoring-dataverse
  environment_id: 89a7081d-6701-f111-8c79-6045bd005919
  environment_url: https://org89912357.crm.dynamics.com
  kind: dataverse_solution
  name: automation-solution-target
  publisher_name: AnandPOC
  publisher_prefix: aks
  solution: automation-solution
  solution_unique_name: ContosoServiceAutomation
  unpack_path: src/solutions/ContosoServiceAutomation
authentication_policy: reuse_if_valid
satisfies:
- INTK-0001-REQ-033
- INTK-0001-REQ-034
- INTK-0001-REQ-042
- INTK-0001-REQ-044
depends_on:
- DEV-0046
- DEV-0047
- DEV-0048
- DEV-0049
- DEV-0051
status: ready
source_plan_hash: 0378f0cb5157ed16a6ad65aed6aae25c4da68e1a7d28116e71ab2a9bf6198d21
task_context_hash: d43afdea72d8505f0c2daaa75ad7174a8ac02da401ab6e6ab278c48510ac0d12
---
# DEV-0053 — DES-05-CMP-009

Generated fields are compiler-owned. Edit only AUTHOR sections.

## Component payload

<!-- COMPILER:BEGIN component-payload -->
```yaml
actions:
- exit succeeded when aks_urgentalertcreated is true
- derive immutable urgent-alert and failure keys from aks_maintenancejobid
- read any existing task by aks_followupkey and reconcile its marker as idempotent
  success
- perform one changeset that creates a High-priority owner-assigned urgent_alert task
  carrying vehicle and scheduled-date context and sets aks_urgentalertcreated true
- retry transient Dataverse actions with exponential backoff
- on failure or timeout, upsert one owner-assigned automation_failure task by failure
  key with sanitized flow-run correlation and terminate Failed
name: Create Urgent Maintenance Job Alert
satisfies:
- INTK-0001-REQ-033
- INTK-0001-REQ-034
- INTK-0001-REQ-042
- INTK-0001-REQ-044
schema_name: aks_CreateUrgentMaintenanceJobAlert
trigger:
  concurrency: 1
  connection_reference: aks_DataverseAutomatedFollowUp
  connector: shared_commondataserviceforapps
  event: row_added
  filter_rows: aks_priority eq 74884 or aks_priority eq 74885
  scope: organization
  table: aks_maintenancejob
```
<!-- COMPILER:END component-payload -->

## Execution profile

<!-- COMPILER:BEGIN execution-profile -->
```yaml
evidence:
- validation_results
- run_history
executor_default: hybrid
source_sync_evidence:
- changed_solution_paths
target_roots:
- src/solutions
validation_kinds:
- payload_conformance
- flow_definition
- connection_reference
- runtime_verification
```
<!-- COMPILER:END execution-profile -->

## Development resources

<!-- COMPILER:BEGIN development-resources -->
```yaml
capabilities:
  implementation_scope: repository_and_dataverse_solution
  matrix_hash: ddc580dd87dc6bd0136b0757f75ca723f7e8256fb15350017a5bbf75137b6bd3
  operations:
    create:
      executor:
        builder: unsupported
        supported: false
      fallback:
        allowed_when: documented_operation_unsupported_or_primary_unavailable
        issue_evidence_required: true
        resource: maker-portal
      official_references:
      - workflow
      - power_automate_flowagent
      operation: any
      primary_resource: power-automate-flowagent
      profile: flowagent-primary
      rationale: Cloud-flow authoring belongs to the Microsoft FlowAgent product resource,
        not the Dataverse HTTP executor; FlowAgent constructs and validates complete
        definitions without fabricated workflow clientdata.
      solution_context:
        mechanism: not_applicable
      support: unsupported_or_not_applicable
      verification:
        rationale: Verify the exact flow definition, publication state, and targeted
          runtime outcome through FlowAgent.
        resource: power-automate-flowagent
        tools:
        - get_flow
        - get_run_history
        - diagnose_run
    delete:
      executor:
        builder: unsupported
        supported: false
      fallback:
        allowed_when: documented_operation_unsupported_or_primary_unavailable
        issue_evidence_required: true
        resource: maker-portal
      official_references:
      - workflow
      - power_automate_flowagent
      operation: any
      primary_resource: power-automate-flowagent
      profile: flowagent-primary
      rationale: Cloud-flow authoring belongs to the Microsoft FlowAgent product resource,
        not the Dataverse HTTP executor; FlowAgent constructs and validates complete
        definitions without fabricated workflow clientdata.
      solution_context:
        mechanism: not_applicable
      support: unsupported_or_not_applicable
      verification:
        rationale: Verify the exact flow definition, publication state, and targeted
          runtime outcome through FlowAgent.
        resource: power-automate-flowagent
        tools:
        - get_flow
        - get_run_history
        - diagnose_run
    update:
      executor:
        builder: unsupported
        supported: false
      fallback:
        allowed_when: documented_operation_unsupported_or_primary_unavailable
        issue_evidence_required: true
        resource: maker-portal
      official_references:
      - workflow
      - power_automate_flowagent
      operation: any
      primary_resource: power-automate-flowagent
      profile: flowagent-primary
      rationale: Cloud-flow authoring belongs to the Microsoft FlowAgent product resource,
        not the Dataverse HTTP executor; FlowAgent constructs and validates complete
        definitions without fabricated workflow clientdata.
      solution_context:
        mechanism: not_applicable
      support: unsupported_or_not_applicable
      verification:
        rationale: Verify the exact flow definition, publication state, and targeted
          runtime outcome through FlowAgent.
        resource: power-automate-flowagent
        tools:
        - get_flow
        - get_run_history
        - diagnose_run
    verify:
      executor:
        builder: unsupported
        supported: false
      fallback:
        allowed_when: documented_operation_unsupported_or_primary_unavailable
        issue_evidence_required: true
        resource: maker-portal
      official_references:
      - workflow
      - power_automate_flowagent
      operation: any
      primary_resource: power-automate-flowagent
      profile: flowagent-primary
      rationale: Cloud-flow authoring belongs to the Microsoft FlowAgent product resource,
        not the Dataverse HTTP executor; FlowAgent constructs and validates complete
        definitions without fabricated workflow clientdata.
      solution_context:
        mechanism: not_applicable
      support: unsupported_or_not_applicable
      verification:
        rationale: Verify the exact flow definition, publication state, and targeted
          runtime outcome through FlowAgent.
        resource: power-automate-flowagent
        tools:
        - get_flow
        - get_run_history
        - diagnose_run
  policy:
    delete_semantics:
      delete: HTTP DELETE removes the metadata component or row from the environment;
        it is destructive and is never a substitute for removing solution membership.
      remove_solution_component: RemoveSolutionComponent removes only the component's
        membership from the named unmanaged solution; it does not delete the component
        from the environment.
    destructive_approval_required: true
    mcp_solution_authoring_rationale: Generic Dataverse MCP writes do not provide
      a compiler-validated explicit custom-solution context equivalent to the operation's
      documented Web API solution mechanism; Dataverse MCP is discovery and targeted
      post-action verification only for solution-aware authoring.
    portal_fallback_evidence_required: true
fallback:
- authentication: interactive_user
  description: Human maker authoring surface.
  endpoint: https://make.powerapps.com
  id: maker-portal
  kind: portal
  purpose: approved_visual_fallback
  rationale: Portal fallback is allowed only when FlowAgent is unavailable or rejects
    an unsupported operation and sanitized issue evidence records the blocker.
  supported_execution_hosts:
  - local_interactive
  version: service-managed
  version_policy: service_managed
preferred:
- authentication: interactive_user
  command: pac
  description: Power Platform CLI for solution source and supported authoring.
  id: power-platform-cli
  kind: cli
  purpose: supported_solution_operations
  rationale: PAC is used only for documented solution/source operations and not generic
    flow creation.
  supported_execution_hosts:
  - local_interactive
  version: 2.9.3
  version_policy: project_pinned
registry_hash: 1bf76cdd7f052634bf775f7f923d80fabbc36ce40e9c96e0d1bea7c5c78bfebc
required:
- authentication: none
  description: Official Microsoft implementation grounding.
  id: microsoft-learn
  kind: mcp
  purpose: flow_grounding
  rationale: Official Power Automate and Workflow references ground the supported
    route.
  server: microsoft-learn
  supported_execution_hosts:
  - local_interactive
  - github_copilot_cloud
  version: service-managed
  version_policy: service_managed
- authentication: interactive_user
  description: Microsoft Power Automate plugin FlowAgent MCP for cloud-flow authoring,
    validation, publishing, execution, and diagnosis.
  id: power-automate-flowagent
  kind: mcp
  purpose: primary_flow_authoring
  rationale: The Microsoft FlowAgent MCP owns validated flow definitions and lifecycle
    operations without fabricated clientdata.
  required_tools:
  - resolve_environment
  - set_current_env
  - get_current_env
  - list_connections
  - pick_or_create_connection
  - get_connector
  - get_operation_details
  - resolve_entity
  - resolve_refs
  - resolve_params
  - validate_flow
  - preflight_flow
  - create_flow
  - update_flow
  - edit_flow
  - delete_flow
  - get_flow
  - publish_flow
  - run_flow
  - smoke_test
  - get_run_history
  - diagnose_run
  server: flowagent
  supported_execution_hosts:
  - local_interactive
  tools:
  - resolve_environment
  - set_current_env
  - get_current_env
  - list_connections
  - pick_or_create_connection
  - get_connector
  - get_operation_details
  - resolve_entity
  - resolve_refs
  - resolve_params
  - validate_flow
  - preflight_flow
  - create_flow
  - update_flow
  - edit_flow
  - delete_flow
  - get_flow
  - publish_flow
  - run_flow
  - smoke_test
  - get_run_history
  - diagnose_run
  version: 3.0.1
  version_policy: exact
- authentication: interactive_user
  description: GA Dataverse MCP endpoint; tool surface is probed at runtime.
  endpoint: https://org89912357.crm.dynamics.com/api/mcp
  id: dataverse-mcp
  kind: mcp
  purpose: schema_and_runtime_verification
  rationale: MCP provides targeted discovery and verification only, not solution-aware
    writes.
  release_channel: ga
  required_tools:
  - search
  - describe
  - read_query
  server: dataverse-authoring
  supported_execution_hosts:
  - local_interactive
  tools:
  - search_data
  - search
  - create_record
  - update_record
  - delete_record
  - create_table
  - update_table
  - delete_table
  - read_query
  - describe
  - upsert_skill
  - delete_skill
  - init_file_upload
  - commit_file_upload
  - file_download
  version: ga
  version_policy: service_managed
route_rationale: Microsoft FlowAgent owns complete cloud-flow definition construction,
  connector discovery, validation, solution preflight, creation, publishing, execution,
  and diagnosis.
```
<!-- COMPILER:END development-resources -->

## Developer preflight

<!-- COMPILER:BEGIN developer-preflight -->
Required execution host: `local_interactive`.
Effective authentication policy: `reuse_if_valid`. Reuse a verified existing session; if it is absent, expired, mismatched, or under-permissioned, complete interactive authentication and revalidate.
`ready` means ready to attempt the exact scoped action. It does not prove that the live environment, solution, publisher, component, or permissions exist.
Broad discovery, solution export/unpack, absence scans, and cross-solution membership scans are not pre-action gates.

### 1. Availability and version

- **microsoft-learn.server-registration** — Verify the exact MCP server key is registered and reachable. (`required`, `automatic`, blocking)
  - Installation: `not_applicable` — Configure the official Microsoft Learn MCP endpoint in the active MCP client.
  - Server: `microsoft-learn`
  - Version: `service-managed`
  - Rationale: Official Power Automate and Workflow references ground the supported route.
- **power-automate-flowagent.flowagent-registration** — Verify the exact flowagent MCP server from the Microsoft Power Automate plugin is registered and reports version 3.0.1. (`required`, `automatic`, blocking)
  - Installation: `manual` — Install power-automate@power-platform-skills 3.0.1 so its self-contained server/mcp.mjs bundle registers the flowagent MCP; Node.js 18+ and Azure CLI are required, with no npm install.
  - Server: `flowagent`
  - Version: `3.0.1`
  - Rationale: The Microsoft FlowAgent MCP owns validated flow definitions and lifecycle operations without fabricated clientdata.
- **dataverse-mcp.server-registration** — Verify the configured server key and resolved GA endpoint. (`required`, `automatic`, blocking)
  - Installation: `not_applicable` — Enable Dataverse MCP in the declared environment and register its GA endpoint in the active MCP client.
  - Server: `dataverse-authoring`
  - Endpoint: `https://org89912357.crm.dynamics.com/api/mcp`
  - Version: `ga`
  - Rationale: MCP provides targeted discovery and verification only, not solution-aware writes.
- **power-platform-cli.pac-version** — Run bare pac because PAC 2.9.3 reports its version and exits successfully, while pac --version is rejected by its command parser; require the exact pinned version. (`preferred`, `automatic`, non-blocking unless selected)
  - Installation: `manual` — Install the project-approved Microsoft.PowerApps.CLI.Tool 2.9.3 package or an equivalent official PAC distribution.
  - Command: `pac`
  - Expected: `2.9.3`
  - Rationale: PAC is used only for documented solution/source operations and not generic flow creation.
- **maker-portal.portal-root** — Validate the official Maker portal HTTPS root. (`fallback`, `automatic`, non-blocking unless selected)
  - Installation: `not_applicable` — Use a supported browser; no local package is installed.
  - Endpoint: `https://make.powerapps.com`
  - Version: `service-managed`
  - Rationale: Portal fallback is allowed only when FlowAgent is unavailable or rejects an unsupported operation and sanitized issue evidence records the blocker.

### 2. MCP and API capability

- **microsoft-learn.documentation-search** — Probe the server tool surface before implementation grounding. (`required`, `automatic`, blocking)
  - Server: `microsoft-learn`
  - Version: `service-managed`
  - Rationale: Official Power Automate and Workflow references ground the supported route.
- **power-automate-flowagent.cloud-flow-tools** — Verify the required environment, connector, validation, create, edit, publish, run, and diagnosis tools are callable. (`required`, `automatic`, blocking)
  - Server: `flowagent`
  - Version: `3.0.1`
  - Required tools: `resolve_environment`, `set_current_env`, `get_current_env`, `list_connections`, `pick_or_create_connection`, `get_connector`, `get_operation_details`, `resolve_entity`, `resolve_refs`, `resolve_params`, `validate_flow`, `preflight_flow`, `create_flow`, `update_flow`, `edit_flow`, `delete_flow`, `get_flow`, `publish_flow`, `run_flow`, `smoke_test`, `get_run_history`, `diagnose_run`
  - Rationale: The Microsoft FlowAgent MCP owns validated flow definitions and lifecycle operations without fabricated clientdata.
- **dataverse-mcp.required-tools** — Verify every task-routed Dataverse MCP tool is exposed. (`required`, `automatic`, blocking)
  - Server: `dataverse-authoring`
  - Endpoint: `https://org89912357.crm.dynamics.com/api/mcp`
  - Version: `ga`
  - Required tools: `search`, `describe`, `read_query`
  - Rationale: MCP provides targeted discovery and verification only, not solution-aware writes.

### 3. Interactive authentication

- **power-automate-flowagent.azure-cli-sign-in** — Authenticate the current human through the FlowAgent Azure CLI and MSAL flow. (`required`, `interactive`, blocking)
  - Existing-session verification: Call get_current_env and confirm its environment ID matches authoring_target.environment_id; resolve and select the compiler-owned environment when no current environment is set.
  - Fresh authentication: host/client cannot be forced programmatically.
  - Fresh-auth action: Sign out only the FlowAgent Azure CLI session, complete a fresh interactive sign-in, and reselect the compiler-owned environment before resuming.
  - Human action: Complete Azure CLI interactive sign-in, then let FlowAgent resolve the exact compiler-owned environment; never record tokens or account output.
  - Rationale: The Microsoft FlowAgent MCP owns validated flow definitions and lifecycle operations without fabricated clientdata.
- **dataverse-mcp.interactive-sign-in** — Authenticate the current human directly to the declared Dataverse environment. (`required`, `interactive`, blocking)
  - Existing-session verification: In the active MCP host, inspect the dataverse-authoring connection and confirm the signed-in user and declared environment without reconnecting.
  - Fresh authentication: host/client cannot be forced programmatically.
  - Fresh-auth action: In the active MCP host, disconnect or sign out only the dataverse-authoring connection, reconnect the declared /api/mcp endpoint, and complete browser or device sign-in before resuming.
  - Endpoint: `https://org89912357.crm.dynamics.com/api/mcp`
  - Human action: Complete the MCP browser or device sign-in and return only non-secret identity results.
  - Rationale: MCP provides targeted discovery and verification only, not solution-aware writes.
- **power-platform-cli.dataverse-auth** — Launch PAC interactive authentication directly against the declared environment. (`preferred`, `interactive`, non-blocking unless selected)
  - Command: `pac auth create --environment https://org89912357.crm.dynamics.com`
  - Existing-session probe: `pac org who`
  - Existing-session verification: Verify that the active PAC session resolves to the declared environment before reuse.
  - Fresh authentication: runner can launch the approved command.
  - Fresh-auth action: Run the approved pac auth create command and complete a new browser sign-in for the declared environment.
  - Human action: Finish browser sign-in, then resume and verify the connected URL and organization ID.
  - Rationale: PAC is used only for documented solution/source operations and not generic flow creation.
- **maker-portal.maker-sign-in** — Sign in as the current human and select the declared environment. (`fallback`, `interactive`, non-blocking unless selected)
  - Existing-session verification: Confirm the current Maker portal user and environment selector before reusing the browser session.
  - Fresh authentication: host/client cannot be forced programmatically.
  - Fresh-auth action: Sign out only the current Maker portal account or open a new private browser session, sign in interactively, and select the declared environment before resuming.
  - Endpoint: `https://make.powerapps.com`
  - Human action: Complete browser sign-in and verify the environment selector before continuing.
  - Rationale: Portal fallback is allowed only when FlowAgent is unavailable or rejects an unsupported operation and sanitized issue evidence records the blocker.

### 4. Authenticated endpoint session

- **authentication.endpoint-session** — Confirm an authenticated, callable session exists for an exact compiler-routed endpoint without broad environment, solution, publisher, component, membership, or permission discovery. (`required`, `automatic`, blocking)
  - Accepted authenticated endpoints: `https://org89912357.crm.dynamics.com/api/mcp`
  - Authentication resources: `dataverse-mcp`, `power-automate-flowagent`

### 5. Compiler-owned target configuration

- **target.compiler-configuration** — Require concrete compiler-owned environment, routed target, canonical identity, and interactive authentication configuration. Do not live-enumerate or preverify their existence. (`required`, `automatic`, blocking)
  - Environment URL: `https://org89912357.crm.dynamics.com`
  - Environment ID: `89a7081d-6701-f111-8c79-6045bd005919`
  - Solution: `ContosoServiceAutomation`
  - Publisher: `AnandPOC`
  - Publisher prefix: `aks`
  - Component identity: `aks_CreateUrgentMaintenanceJobAlert`

### 6. Ready to attempt

- **ready-to-attempt.required-safety** — Permit the first scoped platform action only after host/tool, live Project and compiler integrity, resource/version, endpoint-session, and structural target checks pass. (`required`, `automatic`, blocking)

### Direct scoped action

- Invoke only the DEV/build-skill action against the exact compiler-owned endpoint and target; never fall back to another environment or solution.
- Do not export or unpack a solution, enumerate solution/publisher inventories, scan component absence, or inspect cross-solution membership before the first action.
- Surface sanitized platform/MCP/PAC errors and stop without further writes; never create or repair environment, publisher, or solution prerequisites.
- Route rationale: Microsoft FlowAgent owns complete cloud-flow definition construction, connector discovery, validation, solution preflight, creation, publishing, execution, and diagnosis.
- The operation primary is selected from the compiler-owned capability matrix; Maker Portal cannot be selected for convenience.
- Portal fallback requires sanitized issue evidence naming the exact API failure or unsupported capability.
- Environment URL: `https://org89912357.crm.dynamics.com`
- Routed solution: `ContosoServiceAutomation`
- Canonical identity: `schema_name=aks_CreateUrgentMaintenanceJobAlert`
- Resource action **maker-portal.maker-action** — Perform only the DEV-approved visual authoring or review step.

#### Capability routes

- `create` → `power-automate-flowagent` (`unsupported_or_not_applicable`; no Web API request); Cloud-flow authoring belongs to the Microsoft FlowAgent product resource, not the Dataverse HTTP executor; FlowAgent constructs and validates complete definitions without fabricated workflow clientdata.
- `delete` → `power-automate-flowagent` (`unsupported_or_not_applicable`; no Web API request); Cloud-flow authoring belongs to the Microsoft FlowAgent product resource, not the Dataverse HTTP executor; FlowAgent constructs and validates complete definitions without fabricated workflow clientdata.
- `update` → `power-automate-flowagent` (`unsupported_or_not_applicable`; no Web API request); Cloud-flow authoring belongs to the Microsoft FlowAgent product resource, not the Dataverse HTTP executor; FlowAgent constructs and validates complete definitions without fabricated workflow clientdata.
- `verify` → `power-automate-flowagent` (`unsupported_or_not_applicable`; no Web API request); Cloud-flow authoring belongs to the Microsoft FlowAgent product resource, not the Dataverse HTTP executor; FlowAgent constructs and validates complete definitions without fabricated workflow clientdata.

### Post-action verification

- After a successful write, target-read the exact canonical identity and verify the expected DEV payload outcomes.
- Verify membership in the routed custom solution and absence from other declared custom solutions only after the write or for explicit conflict diagnosis; block mismatches without repair.
- Record sanitized non-secret evidence. Full solution export/unpack is not a verification mechanism.
Registry SHA-256: `1bf76cdd7f052634bf775f7f923d80fabbc36ce40e9c96e0d1bea7c5c78bfebc`. Capability matrix SHA-256: `ddc580dd87dc6bd0136b0757f75ca723f7e8256fb15350017a5bbf75137b6bd3`. This compiler-owned snapshot contains no credentials.
<!-- COMPILER:END developer-preflight -->

## Implementation Notes

<!-- AUTHOR:BEGIN implementation-notes -->
<!-- AUTHOR:END implementation-notes -->

## Checkpoints

<!-- AUTHOR:BEGIN checkpoints -->
<!-- AUTHOR:END checkpoints -->

## Evidence

<!-- AUTHOR:BEGIN evidence -->
<!-- AUTHOR:END evidence -->
