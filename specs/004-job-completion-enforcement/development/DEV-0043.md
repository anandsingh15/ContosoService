---
id: DEV-0043
feature: FEAT-04
plan: DES-04
component: DES-04-CMP-002
component_type: code_webres_js
executor: agent
owner: null
build_skill: dataverse-procode
implementation_scope: repository_and_dataverse_solution
execution_host: local_interactive
authoring_target:
  authentication_mode: interactive_user
  component_projects:
  - component_type: code_webres_*
    path: src/webresources
    project_type: web_resource_source
  connection_alias: dataverse-authoring
  environment: authoring-dataverse
  environment_id: 89a7081d-6701-f111-8c79-6045bd005919
  environment_url: https://org89912357.crm.dynamics.com
  kind: dataverse_solution
  name: webresource-solution-target
  publisher_name: AnandPOC
  publisher_prefix: aks
  solution: webresource-solution
  solution_unique_name: ContosoServiceWebResource
  unpack_path: src/solutions/ContosoServiceWebResource
authentication_policy: reuse_if_valid
satisfies:
- INTK-0001-REQ-027
- INTK-0001-REQ-028
- INTK-0001-REQ-029
- INTK-0001-REQ-045
depends_on: []
status: ready
source_plan_hash: f3576c7636637d30d5ae665dda1b6277c2ea498336db10bc86911d6885d4ec71
task_context_hash: 3a4800a07895989f1d58d10d2082e0faff97183af91954ac9770cd45323e6bee
---
# DEV-0043 — DES-04-CMP-002

Generated fields are compiler-owned. Edit only AUTHOR sections.

## Component payload

<!-- COMPILER:BEGIN component-payload -->
```yaml
behavior:
- use the supported Xrm WebApi to read aks_status and aks_roadworthy for the selected
  vehicle
- show and clear stable form notifications for non-roadworthy or retired vehicles
- prompt immediately when stage 74880 (Completed) has no completion date
- flag a past scheduled date on a new job and synchronously cancel save until corrected
handlers:
- event: OnLoad
  function: Contoso.MaintenanceJobForm.onLoad
  pass_execution_context: true
- event: aks_vehicleid.OnChange
  function: Contoso.MaintenanceJobForm.onVehicleChange
  pass_execution_context: true
- event: aks_stage.OnChange
  function: Contoso.MaintenanceJobForm.onStageChange
  pass_execution_context: true
- event: aks_scheduleddate.OnChange
  function: Contoso.MaintenanceJobForm.onScheduledDateChange
  pass_execution_context: true
- event: OnSave
  function: Contoso.MaintenanceJobForm.onSave
  pass_execution_context: true
name: aks_/scripts/maintenance_job_form.js
namespace: Contoso.MaintenanceJobForm
satisfies:
- INTK-0001-REQ-027
- INTK-0001-REQ-028
- INTK-0001-REQ-029
- INTK-0001-REQ-045
schema_name: aks_/scripts/maintenance_job_form.js
source_path: scripts/maintenance_job_form.js
```
<!-- COMPILER:END component-payload -->

## Execution profile

<!-- COMPILER:BEGIN execution-profile -->
```yaml
evidence:
- changed_paths
- build_results
- test_results
executor_default: agent
target_roots:
- src/plugins
- src/pcf
- src/webresources
validation_kinds:
- payload_conformance
- build
- unit_test
- registration_conformance
```
<!-- COMPILER:END execution-profile -->

## Development resources

<!-- COMPILER:BEGIN development-resources -->
```yaml
capabilities:
  implementation_scope: repository_and_dataverse_solution
  matrix_hash: 9e7701158e417efe11a753227d2e18d74264bf824d35749e8c352d060c6bb8b9
  operations:
    create:
      executor:
        builder: web_resource
        supported: true
      fallback:
        allowed_when: documented_operation_unsupported_or_primary_unavailable
        issue_evidence_required: true
        resource: maker-portal
      http:
        endpoint_family: entity_set
        method: POST
        path_template: webresourceset
      official_references:
      - web_resource
      - optional_parameters
      operation: create
      primary_resource: dataverse-web-api
      profile: web-resource-create
      rationale: Create the exact compiler-routed web resource from its repository
        source with explicit custom-solution context.
      solution_context:
        mechanism: MSCRM.SolutionUniqueName
        name: MSCRM.SolutionUniqueName
        value_source: authoring_target.solution_unique_name
      support: supported
      verification:
        http:
          endpoint_family: entity_set
          method: GET
          path_template: webresourceset
        rationale: Resolve and verify the exact web-resource identity, type, content,
          and routed solution membership.
        resource: dataverse-web-api
    delete:
      destructive_effect: environment_delete
      executor:
        builder: web_resource
        supported: true
      fallback:
        allowed_when: documented_operation_unsupported_or_primary_unavailable
        issue_evidence_required: true
        resource: maker-portal
      http:
        endpoint_family: entity_set
        method: DELETE
        path_template: webresourceset({record_id})
      official_references:
      - web_resource
      operation: delete
      primary_resource: dataverse-web-api
      profile: web-resource-delete
      rationale: Delete only the exact compiler-routed web-resource row after explicit
        destructive approval.
      solution_context:
        mechanism: MSCRM.SolutionUniqueName
        name: MSCRM.SolutionUniqueName
        value_source: authoring_target.solution_unique_name
      support: supported
      verification:
        http:
          endpoint_family: entity_set
          method: GET
          path_template: webresourceset
        rationale: Verify that the exact web-resource identity is absent after approved
          deletion.
        resource: dataverse-web-api
    publish:
      executor:
        builder: web_resource
        supported: true
      fallback:
        allowed_when: documented_operation_unsupported_or_primary_unavailable
        issue_evidence_required: true
        resource: maker-portal
      http:
        endpoint_family: unbound_action
        method: POST
        path_template: PublishXml
      official_references:
      - web_resource
      - publish_xml
      - publish_request_schema
      operation: publish
      primary_resource: dataverse-web-api
      profile: web-resource-publish
      rationale: Publish only the exact updated web resource by immutable ID through
        PublishXml.
      solution_context:
        mechanism: not_applicable
      support: supported
      verification:
        http:
          endpoint_family: entity_set
          method: GET
          path_template: webresourceset
        rationale: Target-read the exact web resource after publishing only its immutable
          ID.
        resource: dataverse-web-api
    update:
      executor:
        builder: web_resource
        supported: true
      fallback:
        allowed_when: documented_operation_unsupported_or_primary_unavailable
        issue_evidence_required: true
        resource: maker-portal
      http:
        endpoint_family: entity_set
        method: PATCH
        path_template: webresourceset({record_id})
      official_references:
      - web_resource
      - optional_parameters
      operation: update
      primary_resource: dataverse-web-api
      profile: web-resource-update
      rationale: Update only the exact compiler-routed web resource from its repository
        source under explicit custom-solution context.
      solution_context:
        mechanism: MSCRM.SolutionUniqueName
        name: MSCRM.SolutionUniqueName
        value_source: authoring_target.solution_unique_name
      support: supported
      verification:
        http:
          endpoint_family: entity_set
          method: GET
          path_template: webresourceset
        rationale: Resolve and verify the exact updated web-resource identity, type,
          content, and routed solution membership.
        resource: dataverse-web-api
    verify:
      executor:
        builder: web_resource
        supported: true
      fallback:
        allowed_when: documented_operation_unsupported_or_primary_unavailable
        issue_evidence_required: true
        resource: maker-portal
      http:
        endpoint_family: entity_set
        method: GET
        path_template: webresourceset
      official_references:
      - web_resource
      operation: verify
      primary_resource: dataverse-web-api
      profile: web-resource-verify
      rationale: Verify the exact web resource without broad solution or component
        discovery.
      solution_context:
        mechanism: not_applicable
      support: supported
      verification:
        http:
          endpoint_family: entity_set
          method: GET
          path_template: webresourceset
        rationale: Target-read the exact canonical web-resource name and verify its
          payload and routed solution membership.
        resource: dataverse-web-api
  policy:
    delete_semantics:
      delete: HTTP DELETE removes the metadata component or row from the environment;
        it is destructive and is never a substitute for removing solution membership.
      remove_solution_component: RemoveSolutionComponent removes only the component's
        membership from the named unmanaged solution; it does not delete the component
        from the environment.
    destructive_approval_required: true
    mcp_solution_authoring_rationale: Generic Dataverse MCP writes do not provide
      a compiler-validated explicit custom-solution context equivalent to the operation's
      documented Web API solution mechanism; Dataverse MCP is discovery and targeted
      post-action verification only for solution-aware authoring.
    portal_fallback_evidence_required: true
registry_hash: 956cd73b0c26532d133ac7cd93f524d1c353dfea73db955037f04b82c21b8347
required:
- authentication: none
  description: Official Microsoft implementation grounding.
  id: microsoft-learn
  kind: mcp
  purpose: web_resource_contract_grounding
  rationale: Official Web Resource and PublishXml documentation grounds the payload,
    solution context, and publication contract.
  server: microsoft-learn
  supported_execution_hosts:
  - local_interactive
  - github_copilot_cloud
  version: service-managed
  version_policy: service_managed
- authentication: none
  description: Project-pinned Node.js and npm toolchain.
  ecosystem: npm
  id: node-toolchain
  kind: sdk
  package: node
  purpose: build_and_test
  rationale: The registered Node toolchain builds and validates web resource content.
  supported_execution_hosts:
  - local_interactive
  - github_copilot_cloud
  version: 24.14.1
  version_policy: project_pinned
- authentication: interactive_user
  description: Capability-gated Dataverse Web API authoring endpoint.
  endpoint: https://org89912357.crm.dynamics.com/api/data/v9.2
  id: dataverse-web-api
  kind: endpoint
  oauth_public_client:
    client_id: 201be4ec-09c9-482a-a3c2-67f3e467d57b
    provider: msal_public_client
    redirect_uri: http://localhost
    scope_suffix: /user_impersonation
    tenant_id: 979fd422-22c4-4a36-bea6-1cf87b6502dd
    token_cache: memory_only
  purpose: solution_aware_web_resource_authoring
  rationale: The compiler-bound executor creates, updates, verifies, and publishes
    the exact web resource with explicit solution context.
  supported_execution_hosts:
  - local_interactive
  version: v9.2
  version_policy: api_contract
- authentication: interactive_user
  command: pac
  description: Power Platform CLI for solution source and supported authoring.
  id: power-platform-cli
  kind: cli
  purpose: post_write_solution_source_sync
  rationale: PAC exports and unpacks the exact routed solution only after successful
    Web API verification.
  supported_execution_hosts:
  - local_interactive
  version: 2.9.3
  version_policy: project_pinned
route_rationale: Web resources use compiler-bound Web API CRUD from routed repository
  source, targeted publication, and PAC only for post-write solution source synchronization.
```
<!-- COMPILER:END development-resources -->

## Developer preflight

<!-- COMPILER:BEGIN developer-preflight -->
Required execution host: `local_interactive`.
Effective authentication policy: `reuse_if_valid`. Reuse a verified existing session; if it is absent, expired, mismatched, or under-permissioned, complete interactive authentication and revalidate.
`ready` means ready to attempt the exact scoped action. It does not prove that the live environment, solution, publisher, component, or permissions exist.
Broad discovery, solution export/unpack, absence scans, and cross-solution membership scans are not pre-action gates.

### 1. Availability and version

- **microsoft-learn.server-registration** — Verify the exact MCP server key is registered and reachable. (`required`, `automatic`, blocking)
  - Installation: `not_applicable` — Configure the official Microsoft Learn MCP endpoint in the active MCP client.
  - Server: `microsoft-learn`
  - Version: `service-managed`
  - Rationale: Official Web Resource and PublishXml documentation grounds the payload, solution context, and publication contract.
- **node-toolchain.node-version** — Require the exact project-pinned Node.js version. (`required`, `automatic`, blocking)
  - Installation: `manual` — Install the exact project-approved Node.js release and retain the repository version file and npm lockfile.
  - Command: `node --version`
  - Expected: `24.14.1`
  - Rationale: The registered Node toolchain builds and validates web resource content.
- **node-toolchain.npm-version** — Require the exact project-pinned npm version. (`required`, `automatic`, blocking)
  - Command: `npm --version`
  - Expected: `11.11.0`
  - Rationale: The registered Node toolchain builds and validates web resource content.
- **dataverse-web-api.service-root** — Validate the resolved HTTPS v9.2 endpoint and API contract. (`required`, `automatic`, blocking)
  - Installation: `not_applicable` — No local install is required; use the declared Dataverse v9.2 service root.
  - Endpoint: `https://org89912357.crm.dynamics.com/api/data/v9.2`
  - Version: `v9.2`
  - Rationale: The compiler-bound executor creates, updates, verifies, and publishes the exact web resource with explicit solution context.
  - OAuth: `msal_public_client`; client ID and tenant are non-secret project prerequisites; token cache is `memory_only`.
- **power-platform-cli.pac-version** — Run bare pac because PAC 2.9.3 reports its version and exits successfully, while pac --version is rejected by its command parser; require the exact pinned version. (`required`, `automatic`, blocking)
  - Installation: `manual` — Install the project-approved Microsoft.PowerApps.CLI.Tool 2.9.3 package or an equivalent official PAC distribution.
  - Command: `pac`
  - Expected: `2.9.3`
  - Rationale: PAC exports and unpacks the exact routed solution only after successful Web API verification.

### 2. MCP and API capability

- **microsoft-learn.documentation-search** — Probe the server tool surface before implementation grounding. (`required`, `automatic`, blocking)
  - Server: `microsoft-learn`
  - Version: `service-managed`
  - Rationale: Official Web Resource and PublishXml documentation grounds the payload, solution context, and publication contract.
- **dataverse-web-api.api-capability** — Confirm the compiler-selected component operation is explicitly supported by the capability matrix. (`required`, `automatic`, blocking)
  - Endpoint: `https://org89912357.crm.dynamics.com/api/data/v9.2`
  - Version: `v9.2`
  - Rationale: The compiler-bound executor creates, updates, verifies, and publishes the exact web resource with explicit solution context.
  - OAuth: `msal_public_client`; client ID and tenant are non-secret project prerequisites; token cache is `memory_only`.

### 3. Interactive authentication

- **dataverse-web-api.interactive-sign-in** — Acquire delegated access for the current human with the configured public-client MSAL registration. (`required`, `interactive`, blocking)
  - Existing-session verification: Use only an in-process silent MSAL lookup to confirm the current user and exact Dataverse resource; never print token data.
  - Fresh authentication: host/client cannot be forced programmatically.
  - Fresh-auth action: Start a new in-memory MSAL public-client session with account selection against the exact Dataverse scope.
  - Endpoint: `https://org89912357.crm.dynamics.com/api/data/v9.2`
  - Human action: Complete interactive OAuth for <environment-url>/user_impersonation; the token cache is memory-only and no token or authentication output is persisted.

ISSUE #22: Cross-Process MSAL Handoff - Consolidated Executor Flow
===========================================================================

Authentication is now consolidated into the executor process to avoid cross-process MSAL cache failures.

Observable Executor States (emitted to stderr as JSON):
1. "waiting-for-human": Emitted before browser OAuth prompt. DEV remains ready.
2. "auth-succeeded": Emitted after successful token acquisition. DEV can transition to in_progress.
3. "auth-failed": Emitted if authentication failed. DEV must remain ready, no action invoked.
4. "request-invoked": Emitted before sending HTTP request. DEV is in_progress, evidence will be required.

Workflow:
1. Readiness gate: Validate DEV and environment are ready
2. Launch executor: python scripts/dataverse_web_api_executor.py <DEV-ID> --operation create --issue-number <N>
3. Monitor stderr: Parse JSON executor_state events
4. On "waiting-for-human": Terminal automation should:
   - Display "waiting for browser authentication..." to user
   - Wait for next state or user interaction
5. On "auth-succeeded": Workflow caller should:
   - Transition DEV to in_progress (auth succeeded, request will be sent)
   - Continue waiting for "request-invoked"
6. On "auth-failed": Workflow caller should:
   - Keep DEV in ready state (no action invoked, no evidence)
   - Retry or escalate per policy
7. On "request-invoked": Web API request is being sent:
   - DEV is already in in_progress
   - Sanitized evidence will be posted to issue
   - If interrupted here, evidence is required
8. Executor completes with success/failure and posts final evidence

DO NOT:
- Call executor with --authenticate-only separately (deprecated per issue #22)
- Transition DEV to in_progress before executor sends request
- Assume success if no evidence is posted to the issue

  - Rationale: The compiler-bound executor creates, updates, verifies, and publishes the exact web resource with explicit solution context.
- **power-platform-cli.dataverse-auth** — Launch PAC interactive authentication directly against the declared environment. (`required`, `interactive`, non-blocking unless selected)
  - Command: `pac auth create --environment https://org89912357.crm.dynamics.com`
  - Existing-session probe: `pac org who`
  - Existing-session verification: Verify that the active PAC session resolves to the declared environment before reuse.
  - Fresh authentication: runner can launch the approved command.
  - Fresh-auth action: Run the approved pac auth create command and complete a new browser sign-in for the declared environment.
  - Human action: Finish browser sign-in, then resume and verify the connected URL and organization ID.
  - Rationale: PAC exports and unpacks the exact routed solution only after successful Web API verification.

### 4. Authenticated endpoint session

- **authentication.endpoint-session** — Confirm an authenticated, callable session exists for an exact compiler-routed endpoint without broad environment, solution, publisher, component, membership, or permission discovery. (`required`, `automatic`, blocking)
  - Accepted authenticated endpoints: `https://org89912357.crm.dynamics.com/api/data/v9.2`
  - Authentication resources: `dataverse-web-api`

### 5. Compiler-owned target configuration

- **target.compiler-configuration** — Require concrete compiler-owned environment, routed target, canonical identity, and interactive authentication configuration. Do not live-enumerate or preverify their existence. (`required`, `automatic`, blocking)
  - Environment URL: `https://org89912357.crm.dynamics.com`
  - Environment ID: `89a7081d-6701-f111-8c79-6045bd005919`
  - Solution: `ContosoServiceWebResource`
  - Publisher: `AnandPOC`
  - Publisher prefix: `aks`
  - Component identity: `aks_/scripts/maintenance_job_form.js`

### 6. Ready to attempt

- **ready-to-attempt.required-safety** — Permit the first scoped platform action only after host/tool, live Project and compiler integrity, resource/version, endpoint-session, and structural target checks pass. (`required`, `automatic`, blocking)

### Direct scoped action

- Invoke only the DEV/build-skill action against the exact compiler-owned endpoint and target; never fall back to another environment or solution.
- Do not export or unpack a solution, enumerate solution/publisher inventories, scan component absence, or inspect cross-solution membership before the first action.
- Surface sanitized platform/MCP/PAC errors and stop without further writes; never create or repair environment, publisher, or solution prerequisites.
- Route rationale: Web resources use compiler-bound Web API CRUD from routed repository source, targeted publication, and PAC only for post-write solution source synchronization.
- The operation primary is selected from the compiler-owned capability matrix; Maker Portal cannot be selected for convenience.
- Portal fallback requires sanitized issue evidence naming the exact API failure or unsupported capability.
- Environment URL: `https://org89912357.crm.dynamics.com`
- Routed solution: `ContosoServiceWebResource`
- Canonical identity: `schema_name=aks_/scripts/maintenance_job_form.js`

#### Capability routes

- `create` → `dataverse-web-api` (`supported`; POST webresourceset); Create the exact compiler-routed web resource from its repository source with explicit custom-solution context.
- `delete` → `dataverse-web-api` (`supported`; DELETE webresourceset({record_id})); Delete only the exact compiler-routed web-resource row after explicit destructive approval.
- `publish` → `dataverse-web-api` (`supported`; POST PublishXml); Publish only the exact updated web resource by immutable ID through PublishXml.
- `update` → `dataverse-web-api` (`supported`; PATCH webresourceset({record_id})); Update only the exact compiler-routed web resource from its repository source under explicit custom-solution context.
- `verify` → `dataverse-web-api` (`supported`; GET webresourceset); Verify the exact web resource without broad solution or component discovery.

### Post-action verification

- After a successful write, target-read the exact canonical identity and verify the expected DEV payload outcomes.
- Verify membership in the routed custom solution and absence from other declared custom solutions only after the write or for explicit conflict diagnosis; block mismatches without repair.
- Record sanitized non-secret evidence. Full solution export/unpack is not a verification mechanism.
Registry SHA-256: `956cd73b0c26532d133ac7cd93f524d1c353dfea73db955037f04b82c21b8347`. Capability matrix SHA-256: `9e7701158e417efe11a753227d2e18d74264bf824d35749e8c352d060c6bb8b9`. This compiler-owned snapshot contains no credentials.
<!-- COMPILER:END developer-preflight -->

## Implementation Notes

<!-- AUTHOR:BEGIN implementation-notes -->
- Add `src/webresources/scripts/maintenance_job_form.js` using the
  `Contoso.MaintenanceJobForm` namespace and only supported Xrm Client API
  calls. Resolve `formContext` from the passed execution context; do not use
  `Xrm.Page`, DOM manipulation, or runtime event registration.
- Implement the declared OnLoad, vehicle/stage/scheduled-date OnChange, and
  synchronous OnSave entry points. Use stable notification IDs, clear stale
  notifications, inspect save mode, and call `preventDefault` only for the
  declared past-date save guard.
- Keep client behavior advisory for vehicle/completion guidance; the DEV-0042
  plug-in remains authoritative for every save route.
- Add deterministic JavaScript tests with mocked execution context, form
  attributes, notifications, `Xrm.WebApi`, and save-event arguments for retired
  and non-roadworthy vehicles, completion-date prompting, valid and past dates,
  notification clearing, and Web API failure handling.
<!-- AUTHOR:END implementation-notes -->

## Checkpoints

<!-- AUTHOR:BEGIN checkpoints -->
- [ ] Agent: JavaScript validation/tests pass and no unsupported client API or
  duplicate runtime handler registration is present.
- [ ] Agent: `aks_/scripts/maintenance_job_form.js` is published in
  `ContosoServiceWebResource` and target-read by canonical name.
<!-- AUTHOR:END checkpoints -->

## Evidence

<!-- AUTHOR:BEGIN evidence -->
- Changed source and test paths under `src/webresources`.
- JavaScript validation and unit-test results for every declared handler.
- Sanitized execution evidence for web-resource publication, targeted runtime
  verification, routed-solution membership, and changed source-sync paths.
<!-- AUTHOR:END evidence -->
