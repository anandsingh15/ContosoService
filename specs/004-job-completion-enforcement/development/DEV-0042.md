---
id: DEV-0042
feature: FEAT-04
plan: DES-04
component: DES-04-CMP-001
component_type: code_plugin
executor: agent
owner: null
build_skill: dataverse-procode
implementation_scope: repository_and_dataverse_solution
execution_host: local_interactive
authoring_target:
  authentication_mode: interactive_user
  component_projects:
  - component_type: code_plugin
    path: src/plugins/ContosoService.Plugins
    project_type: dotnet_class_library
  connection_alias: dataverse-authoring
  environment: authoring-dataverse
  environment_id: 89a7081d-6701-f111-8c79-6045bd005919
  environment_url: https://org89912357.crm.dynamics.com
  kind: dataverse_solution
  name: plugin-customapi-solution-target
  publisher_name: AnandPOC
  publisher_prefix: aks
  solution: plugin-customapi-solution
  solution_unique_name: ContosoServicePluginAndCustomApi
  unpack_path: src/solutions/ContosoServicePluginAndCustomApi
authentication_policy: reuse_if_valid
satisfies:
- INTK-0001-REQ-022
- INTK-0001-REQ-023
- INTK-0001-REQ-024
- INTK-0001-REQ-025
- INTK-0001-REQ-026
- INTK-0001-REQ-029
- INTK-0001-REQ-045
depends_on: []
status: in_review
source_plan_hash: 1e8da2d1488fef7df6629fb0cdf2f81733298b0b7a368a52afc1fd1e367fae6d
task_context_hash: e23d672565b8fdd6410453becee20998b92e30cb1f666126705f588b242bec14
---
# DEV-0042 — DES-04-CMP-001

Generated fields are compiler-owned. Edit only AUTHOR sections.

## Component payload

<!-- COMPILER:BEGIN component-payload -->
```yaml
assembly: ContosoService.Plugins
class_name: ContosoService.Plugins.MaintenanceJobCompletionPlugin
name: Maintenance Job completion enforcement
satisfies:
- INTK-0001-REQ-022
- INTK-0001-REQ-023
- INTK-0001-REQ-024
- INTK-0001-REQ-025
- INTK-0001-REQ-026
- INTK-0001-REQ-029
- INTK-0001-REQ-045
schema_name: ContosoService.Plugins.MaintenanceJobCompletionPlugin
steps:
- behavior:
  - reject aks_scheduleddate earlier than the current date
  - reject initial aks_stage 74880 (Completed) because a new job cannot yet have a
    related aks_jobpart
  message: Create
  mode: Synchronous
  name: Maintenance Job Create completion and schedule guard
  rank: 10
  run_as: Calling User
  stage: PreOperation
  table: aks_maintenancejob
- behavior:
  - merge Target with PreImage to evaluate the effective row
  - reject transition to 74880 (Completed) unless the related aks_vehicle is roadworthy,
    not 74876 (Retired), and at least one related aks_jobpart exists
  - when entering Completed without aks_completeddate, set aks_completeddate from
    an injected UTC clock
  - reject any transition from 74880 (Completed) to another stage
  filtering_attributes:
  - aks_stage
  message: Update
  mode: Synchronous
  name: Maintenance Job Update completion and final-state guard
  pre_image:
    alias: PreImage
    columns:
    - aks_stage
    - aks_completeddate
    - aks_scheduleddate
    - aks_vehicleid
  rank: 10
  run_as: Calling User
  stage: PreOperation
  table: aks_maintenancejob
```
<!-- COMPILER:END component-payload -->

## Execution profile

<!-- COMPILER:BEGIN execution-profile -->
```yaml
evidence:
- changed_paths
- build_results
- test_results
executor_default: agent
target_roots:
- src/plugins
- src/pcf
- src/webresources
validation_kinds:
- payload_conformance
- build
- unit_test
- registration_conformance
```
<!-- COMPILER:END execution-profile -->

## Development resources

<!-- COMPILER:BEGIN development-resources -->
```yaml
capabilities:
  implementation_scope: repository_and_dataverse_solution
  matrix_hash: f1937248877075119a1645c6be15300e9b704782d7fd8ce540b63077cde580ff
  operations:
    create:
      executor:
        builder: code_plugin_registration
        supported: true
      fallback:
        allowed_when: documented_operation_unsupported_or_primary_unavailable
        issue_evidence_required: true
        resource: maker-portal
      http:
        endpoint_family: entity_set
        method: POST
        path_template: pluginassemblies
      official_references:
      - optional_parameters
      - plugin_registration
      - plugin_assembly
      - plugin_type
      - sdk_message_processing_step
      - sdk_message_processing_step_image
      - solution_component_types
      operation: create
      primary_resource: dataverse-web-api
      profile: plugin-registration-create
      rationale: Upload the exact signed assembly with explicit solution context,
        resolve its automatically registered plug-in type, then create the compiler-declared
        steps and images as one verified aggregate.
      solution_context:
        mechanism: MSCRM.SolutionUniqueName
        name: MSCRM.SolutionUniqueName
        value_source: authoring_target.solution_unique_name
      support: supported
      verification:
        http:
          endpoint_family: entity_set
          method: GET
          path_template: plugintypes
        rationale: Resolve the exact generated plug-in type, steps, images, and routed
          solution memberships after the aggregate write.
        resource: dataverse-web-api
    delete:
      executor:
        builder: unsupported
        supported: false
      fallback:
        allowed_when: not_applicable
        issue_evidence_required: false
        resource: maker-portal
      official_references:
      - power_platform_cli
      operation: any
      primary_resource: power-platform-cli
      profile: pac-primary
      rationale: PAC remains primary only for its documented source, package, registration,
        and solution operations; generic component creation is not claimed.
      solution_context:
        mechanism: not_applicable
      support: unsupported_or_not_applicable
      verification:
        rationale: Use targeted Dataverse verification after supported PAC source,
          package, or registration operations.
        resource: dataverse-mcp
        tools:
        - describe
        - read_query
    update:
      executor:
        builder: code_plugin_registration
        supported: true
      fallback:
        allowed_when: documented_operation_unsupported_or_primary_unavailable
        issue_evidence_required: true
        resource: maker-portal
      http:
        endpoint_family: entity_set
        method: PATCH
        path_template: pluginassemblies({record_id})
      official_references:
      - optional_parameters
      - plugin_registration
      - plugin_assembly
      - plugin_type
      - sdk_message_processing_step
      - sdk_message_processing_step_image
      - solution_component_types
      operation: update
      primary_resource: dataverse-web-api
      profile: plugin-registration-update
      rationale: Update only the exact assembly and idempotently reconcile its compiler-declared
        steps and image under explicit solution context.
      solution_context:
        mechanism: MSCRM.SolutionUniqueName
        name: MSCRM.SolutionUniqueName
        value_source: authoring_target.solution_unique_name
      support: supported
      verification:
        http:
          endpoint_family: entity_set
          method: GET
          path_template: plugintypes
        rationale: Resolve and verify the exact plug-in registration graph and routed
          solution memberships after the aggregate update.
        resource: dataverse-web-api
    verify:
      executor:
        builder: code_plugin_registration
        supported: true
      fallback:
        allowed_when: documented_operation_unsupported_or_primary_unavailable
        issue_evidence_required: true
        resource: maker-portal
      http:
        endpoint_family: entity_set
        method: GET
        path_template: plugintypes
      official_references:
      - plugin_registration
      - plugin_assembly
      - plugin_type
      - sdk_message_processing_step
      - sdk_message_processing_step_image
      - solution_component_types
      operation: verify
      primary_resource: dataverse-web-api
      profile: plugin-registration-verify
      rationale: Targeted exact-identity reads verify the complete registration graph
        without broad inventory or mutation.
      solution_context:
        mechanism: not_applicable
      support: supported
      verification:
        http:
          endpoint_family: entity_set
          method: GET
          path_template: plugintypes
        rationale: Verify the exact generated plug-in type, declared steps and image,
          and routed solution memberships without writing.
        resource: dataverse-web-api
  policy:
    delete_semantics:
      delete: HTTP DELETE removes the metadata component or row from the environment;
        it is destructive and is never a substitute for removing solution membership.
      remove_solution_component: RemoveSolutionComponent removes only the component's
        membership from the named unmanaged solution; it does not delete the component
        from the environment.
    destructive_approval_required: true
    mcp_solution_authoring_rationale: Generic Dataverse MCP writes do not provide
      a compiler-validated explicit custom-solution context equivalent to the operation's
      documented Web API solution mechanism; Dataverse MCP is discovery and targeted
      post-action verification only for solution-aware authoring.
    portal_fallback_evidence_required: true
registry_hash: 15e4f75195d412beaa83fc4accf9b5650c480cd3ef6748b10f95272ae664023f
required:
- authentication: none
  description: Official Microsoft implementation grounding.
  id: microsoft-learn
  kind: mcp
  purpose: execution_pipeline_grounding
  rationale: Official plug-in documentation grounds registration and runtime behavior.
  server: microsoft-learn
  supported_execution_hosts:
  - local_interactive
  - github_copilot_cloud
  version: service-managed
  version_policy: service_managed
- authentication: none
  description: Project-supported .NET build and test toolchain.
  ecosystem: dotnet
  id: dotnet-toolchain
  kind: sdk
  purpose: build_and_test
  rationale: The pinned .NET toolchain builds and tests the component.
  supported_execution_hosts:
  - local_interactive
  - github_copilot_cloud
  version: 10.0.302
  version_policy: project_pinned
- authentication: none
  description: Dataverse .NET SDK pinned in the project manifest.
  ecosystem: nuget
  id: dataverse-dotnet-sdk
  kind: sdk
  package: Microsoft.PowerPlatform.Dataverse.Client
  purpose: dataverse_code
  rationale: The registered Dataverse SDK supports code integration and registration
    contracts.
  supported_execution_hosts:
  - local_interactive
  version: 1.1.32
  version_policy: project_pinned
- authentication: none
  description: Pinned Microsoft Authentication Library for delegated public-client
    Dataverse authentication.
  ecosystem: pip
  id: msal-python
  kind: sdk
  package: msal
  purpose: delegated_authentication
  rationale: The Web API executor uses the pinned in-memory MSAL public-client flow.
  supported_execution_hosts:
  - local_interactive
  version: 1.37.0
  version_policy: project_pinned
- authentication: interactive_user
  description: Capability-gated Dataverse Web API authoring endpoint.
  endpoint: https://org89912357.crm.dynamics.com/api/data/v9.2
  id: dataverse-web-api
  kind: endpoint
  oauth_public_client:
    client_id: 201be4ec-09c9-482a-a3c2-67f3e467d57b
    provider: msal_public_client
    redirect_uri: http://localhost
    scope_suffix: /user_impersonation
    tenant_id: 979fd422-22c4-4a36-bea6-1cf87b6502dd
    token_cache: memory_only
  purpose: solution_aware_plugin_registration
  rationale: The compiler-bound executor registers the assembly, steps, and image
    with explicit solution context.
  supported_execution_hosts:
  - local_interactive
  version: v9.2
  version_policy: api_contract
- authentication: interactive_user
  description: GA Dataverse MCP endpoint; tool surface is probed at runtime.
  endpoint: https://org89912357.crm.dynamics.com/api/mcp
  id: dataverse-mcp
  kind: mcp
  purpose: schema_and_runtime_verification
  rationale: MCP performs targeted discovery and verification without generic solution-aware
    writes.
  release_channel: ga
  required_tools:
  - search
  - describe
  - read_query
  server: dataverse-authoring
  supported_execution_hosts:
  - local_interactive
  tools:
  - search_data
  - search
  - create_record
  - update_record
  - delete_record
  - create_table
  - update_table
  - delete_table
  - read_query
  - describe
  - upsert_skill
  - delete_skill
  - init_file_upload
  - commit_file_upload
  - file_download
  version: ga
  version_policy: service_managed
route_rationale: Plug-in implementation uses the supported build toolchain and compiler-bound
  solution-aware Web API registration executor.
```
<!-- COMPILER:END development-resources -->

## Developer preflight

<!-- COMPILER:BEGIN developer-preflight -->
Required execution host: `local_interactive`.
Effective authentication policy: `reuse_if_valid`. Reuse a verified existing session; if it is absent, expired, mismatched, or under-permissioned, complete interactive authentication and revalidate.
`ready` means ready to attempt the exact scoped action. It does not prove that the live environment, solution, publisher, component, or permissions exist.
Broad discovery, solution export/unpack, absence scans, and cross-solution membership scans are not pre-action gates.

### 1. Availability and version

- **microsoft-learn.server-registration** — Verify the exact MCP server key is registered and reachable. (`required`, `automatic`, blocking)
  - Installation: `not_applicable` — Configure the official Microsoft Learn MCP endpoint in the active MCP client.
  - Server: `microsoft-learn`
  - Version: `service-managed`
  - Rationale: Official plug-in documentation grounds registration and runtime behavior.
- **dotnet-toolchain.dotnet-version** — Require the exact project-pinned .NET SDK version. (`required`, `automatic`, blocking)
  - Installation: `manual` — Install the exact SDK pinned by global.json from the official .NET distribution.
  - Command: `dotnet --version`
  - Expected: `10.0.302`
  - Rationale: The pinned .NET toolchain builds and tests the component.
- **dataverse-dotnet-sdk.package-version** — Verify the exact resolved NuGet package version in the consuming project. (`required`, `automatic`, blocking)
  - Installation: `manual` — Add the exact Microsoft.PowerPlatform.Dataverse.Client version to the project manifest and lock file.
  - Expected: `1.1.32`
  - Version: `1.1.32`
  - Package: `Microsoft.PowerPlatform.Dataverse.Client`
  - Component projects: `src/plugins/ContosoService.Plugins`
  - Rationale: The registered Dataverse SDK supports code integration and registration contracts.
- **msal-python.msal-version** — Require the exact pinned MSAL Python package without exposing token-cache data. (`required`, `automatic`, blocking)
  - Installation: `manual` — Install exactly msal==1.37.0 in the Python environment used by scripts/dataverse_web_api_executor.py.
  - Command: `python -c import importlib.metadata; print(importlib.metadata.version('msal'))`
  - Expected: `1.37.0`
  - Rationale: The Web API executor uses the pinned in-memory MSAL public-client flow.
- **dataverse-web-api.service-root** — Validate the resolved HTTPS v9.2 endpoint and API contract. (`required`, `automatic`, blocking)
  - Installation: `not_applicable` — No local install is required; use the declared Dataverse v9.2 service root.
  - Endpoint: `https://org89912357.crm.dynamics.com/api/data/v9.2`
  - Version: `v9.2`
  - Rationale: The compiler-bound executor registers the assembly, steps, and image with explicit solution context.
  - OAuth: `msal_public_client`; client ID and tenant are non-secret project prerequisites; token cache is `memory_only`.
- **dataverse-mcp.server-registration** — Verify the configured server key and resolved GA endpoint. (`required`, `automatic`, blocking)
  - Installation: `not_applicable` — Enable Dataverse MCP in the declared environment and register its GA endpoint in the active MCP client.
  - Server: `dataverse-authoring`
  - Endpoint: `https://org89912357.crm.dynamics.com/api/mcp`
  - Version: `ga`
  - Rationale: MCP performs targeted discovery and verification without generic solution-aware writes.

### 2. MCP and API capability

- **microsoft-learn.documentation-search** — Probe the server tool surface before implementation grounding. (`required`, `automatic`, blocking)
  - Server: `microsoft-learn`
  - Version: `service-managed`
  - Rationale: Official plug-in documentation grounds registration and runtime behavior.
- **dataverse-web-api.api-capability** — Confirm the compiler-selected component operation is explicitly supported by the capability matrix. (`required`, `automatic`, blocking)
  - Endpoint: `https://org89912357.crm.dynamics.com/api/data/v9.2`
  - Version: `v9.2`
  - Rationale: The compiler-bound executor registers the assembly, steps, and image with explicit solution context.
  - OAuth: `msal_public_client`; client ID and tenant are non-secret project prerequisites; token cache is `memory_only`.
- **dataverse-mcp.required-tools** — Verify every task-routed Dataverse MCP tool is exposed. (`required`, `automatic`, blocking)
  - Server: `dataverse-authoring`
  - Endpoint: `https://org89912357.crm.dynamics.com/api/mcp`
  - Version: `ga`
  - Required tools: `search`, `describe`, `read_query`
  - Rationale: MCP performs targeted discovery and verification without generic solution-aware writes.

### 3. Interactive authentication

- **dataverse-web-api.interactive-sign-in** — Acquire delegated access for the current human with the configured public-client MSAL registration. (`required`, `interactive`, blocking)
  - Existing-session verification: Use only an in-process silent MSAL lookup to confirm the current user and exact Dataverse resource; never print token data.
  - Fresh authentication: host/client cannot be forced programmatically.
  - Fresh-auth action: Start a new in-memory MSAL public-client session with account selection against the exact Dataverse scope.
  - Endpoint: `https://org89912357.crm.dynamics.com/api/data/v9.2`
  - Human action: Complete interactive OAuth for <environment-url>/user_impersonation; the token cache is memory-only and no token or authentication output is persisted.

ISSUE #22: Cross-Process MSAL Handoff - Consolidated Executor Flow
===========================================================================

Authentication is now consolidated into the executor process to avoid cross-process MSAL cache failures.

Observable Executor States (emitted to stderr as JSON):
1. "waiting-for-human": Emitted before browser OAuth prompt. DEV remains ready.
2. "auth-succeeded": Emitted after successful token acquisition. DEV can transition to in_progress.
3. "auth-failed": Emitted if authentication failed. DEV must remain ready, no action invoked.
4. "request-invoked": Emitted before sending HTTP request. DEV is in_progress, evidence will be required.

Workflow:
1. Readiness gate: Validate DEV and environment are ready
2. Launch executor: python scripts/dataverse_web_api_executor.py <DEV-ID> --operation create --issue-number <N>
3. Monitor stderr: Parse JSON executor_state events
4. On "waiting-for-human": Terminal automation should:
   - Display "waiting for browser authentication..." to user
   - Wait for next state or user interaction
5. On "auth-succeeded": Workflow caller should:
   - Transition DEV to in_progress (auth succeeded, request will be sent)
   - Continue waiting for "request-invoked"
6. On "auth-failed": Workflow caller should:
   - Keep DEV in ready state (no action invoked, no evidence)
   - Retry or escalate per policy
7. On "request-invoked": Web API request is being sent:
   - DEV is already in in_progress
   - Sanitized evidence will be posted to issue
   - If interrupted here, evidence is required
8. Executor completes with success/failure and posts final evidence

DO NOT:
- Call executor with --authenticate-only separately (deprecated per issue #22)
- Transition DEV to in_progress before executor sends request
- Assume success if no evidence is posted to the issue

  - Rationale: The compiler-bound executor registers the assembly, steps, and image with explicit solution context.
- **dataverse-mcp.interactive-sign-in** — Authenticate the current human directly to the declared Dataverse environment. (`required`, `interactive`, blocking)
  - Existing-session verification: In the active MCP host, inspect the dataverse-authoring connection and confirm the signed-in user and declared environment without reconnecting.
  - Fresh authentication: host/client cannot be forced programmatically.
  - Fresh-auth action: In the active MCP host, disconnect or sign out only the dataverse-authoring connection, reconnect the declared /api/mcp endpoint, and complete browser or device sign-in before resuming.
  - Endpoint: `https://org89912357.crm.dynamics.com/api/mcp`
  - Human action: Complete the MCP browser or device sign-in and return only non-secret identity results.
  - Rationale: MCP performs targeted discovery and verification without generic solution-aware writes.

### 4. Authenticated endpoint session

- **authentication.endpoint-session** — Confirm an authenticated, callable session exists for an exact compiler-routed endpoint without broad environment, solution, publisher, component, membership, or permission discovery. (`required`, `automatic`, blocking)
  - Accepted authenticated endpoints: `https://org89912357.crm.dynamics.com/api/data/v9.2`, `https://org89912357.crm.dynamics.com/api/mcp`
  - Authentication resources: `dataverse-mcp`, `dataverse-web-api`

### 5. Compiler-owned target configuration

- **target.compiler-configuration** — Require concrete compiler-owned environment, routed target, canonical identity, and interactive authentication configuration. Do not live-enumerate or preverify their existence. (`required`, `automatic`, blocking)
  - Environment URL: `https://org89912357.crm.dynamics.com`
  - Environment ID: `89a7081d-6701-f111-8c79-6045bd005919`
  - Solution: `ContosoServicePluginAndCustomApi`
  - Publisher: `AnandPOC`
  - Publisher prefix: `aks`
  - Component identity: `ContosoService.Plugins.MaintenanceJobCompletionPlugin`

### 6. Ready to attempt

- **ready-to-attempt.required-safety** — Permit the first scoped platform action only after host/tool, live Project and compiler integrity, resource/version, endpoint-session, and structural target checks pass. (`required`, `automatic`, blocking)

### Direct scoped action

- Invoke only the DEV/build-skill action against the exact compiler-owned endpoint and target; never fall back to another environment or solution.
- Do not export or unpack a solution, enumerate solution/publisher inventories, scan component absence, or inspect cross-solution membership before the first action.
- Surface sanitized platform/MCP/PAC errors and stop without further writes; never create or repair environment, publisher, or solution prerequisites.
- Route rationale: Plug-in implementation uses the supported build toolchain and compiler-bound solution-aware Web API registration executor.
- The operation primary is selected from the compiler-owned capability matrix; Maker Portal cannot be selected for convenience.
- Portal fallback requires sanitized issue evidence naming the exact API failure or unsupported capability.
- Environment URL: `https://org89912357.crm.dynamics.com`
- Routed solution: `ContosoServicePluginAndCustomApi`
- Canonical identity: `schema_name=ContosoService.Plugins.MaintenanceJobCompletionPlugin`

#### Capability routes

- `create` → `dataverse-web-api` (`supported`; POST pluginassemblies); Upload the exact signed assembly with explicit solution context, resolve its automatically registered plug-in type, then create the compiler-declared steps and images as one verified aggregate.
- `delete` → `power-platform-cli` (`unsupported_or_not_applicable`; no Web API request); PAC remains primary only for its documented source, package, registration, and solution operations; generic component creation is not claimed.
- `update` → `dataverse-web-api` (`supported`; PATCH pluginassemblies({record_id})); Update only the exact assembly and idempotently reconcile its compiler-declared steps and image under explicit solution context.
- `verify` → `dataverse-web-api` (`supported`; GET plugintypes); Targeted exact-identity reads verify the complete registration graph without broad inventory or mutation.

### Post-action verification

- After a successful write, target-read the exact canonical identity and verify the expected DEV payload outcomes.
- Verify membership in the routed custom solution and absence from other declared custom solutions only after the write or for explicit conflict diagnosis; block mismatches without repair.
- Record sanitized non-secret evidence. Full solution export/unpack is not a verification mechanism.
Registry SHA-256: `15e4f75195d412beaa83fc4accf9b5650c480cd3ef6748b10f95272ae664023f`. Capability matrix SHA-256: `f1937248877075119a1645c6be15300e9b704782d7fd8ce540b63077cde580ff`. This compiler-owned snapshot contains no credentials.
<!-- COMPILER:END developer-preflight -->

## Implementation Notes

<!-- AUTHOR:BEGIN implementation-notes -->
- Reuse the signed `net462` project at `src/plugins/ContosoService.Plugins` and
  implement `ContosoService.Plugins.MaintenanceJobCompletionPlugin` on the
  existing `PluginBase` abstraction.
- Keep completion enforcement transactional in the two declared synchronous
  PreOperation steps. Merge the Update target with the narrow `PreImage`, query
  only the related vehicle state and existence of an `aks_jobpart`, and set
  `aks_completeddate` through an injectable UTC clock.
- Register the assembly, type, Create step, Update filtering attributes, and
  Update pre-image in `ContosoServicePluginAndCustomApi`, all in calling-user
  context. Do not add an external call or environment-specific configuration.
- Add offline fake-pipeline tests for valid completion, missing parts,
  non-roadworthy/retired vehicles, past scheduling, initial completion,
  reopening, automatic completion date, unchanged-stage no-op, and the exact
  `InvalidPluginExecutionException` messages.
<!-- AUTHOR:END implementation-notes -->

## Checkpoints

<!-- AUTHOR:BEGIN checkpoints -->
- [x] Agent: project build and offline plug-in tests pass.
- [x] Agent: registration matches both declared steps, rank, filtering
  attributes, calling-user context, and narrow `PreImage` columns.
- [x] Human: confirm the user-facing rejection messages are clear in the
  model-driven app before review completion.
<!-- AUTHOR:END checkpoints -->

## Evidence

<!-- AUTHOR:BEGIN evidence -->
- Replaced the template plug-in with
  `ContosoService.Plugins.MaintenanceJobCompletionPlugin` and added the signed,
  locked `ContosoService.Plugins.Tests` project under `src/plugins`.
- `dotnet test src/plugins/ContosoService.Plugins.Tests/ContosoService.Plugins.Tests.csproj
  -c Release --no-restore`: 8 passed, 0 failed, 0 skipped.
- Current-hash Web API update and aggregate verification succeeded with immutable
  assembly ID `c39ed95b-1d9b-f111-b8db-6045bd01d8e8`; sanitized evidence:
  https://github.com/anandsingh15/ContosoService/issues/140#issuecomment-5332027714.
- Post-verification export projected only `PluginAssemblies/` and
  `SdkMessageProcessingSteps/`; rebuilt, exported, and projected DLL SHA-256:
  `6b86543bfbca1ac8caf47473b94297c008f3eb7e0ca8112d1e614b5dc9eefaa3`.
<!-- AUTHOR:END evidence -->
