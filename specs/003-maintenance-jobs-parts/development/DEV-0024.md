---
id: DEV-0024
feature: FEAT-03
plan: DES-03
component: DES-03-CMP-009
component_type: config_audit
executor: hybrid
owner: null
build_skill: dataverse-security
implementation_scope: repository_and_dataverse_environment
execution_host: local_interactive
authoring_target:
  authentication_mode: interactive_user
  connection_alias: dataverse-authoring
  environment: authoring-dataverse
  environment_id: 89a7081d-6701-f111-8c79-6045bd005919
  environment_url: https://org89912357.crm.dynamics.com
  kind: dataverse_environment
  name: dataverse-environment-authoring
authentication_policy: reuse_if_valid
satisfies:
- INTK-0001-REQ-007
- INTK-0001-REQ-011
- INTK-0001-REQ-013
depends_on:
- DEV-0016
- DEV-0017
- DEV-0038
- DEV-0039
- DEV-0040
- DEV-0041
status: ready
source_plan_hash: e9633b0e5ac591825cdcd96de4fe6664c3e9802ed610aab5527236fc56cdf6cf
task_context_hash: d72786ab974f19749be5d9a44cda9082dc4a2418a015c506361302eac050a08d
---
# DEV-0024 — DES-03-CMP-009

Generated fields are compiler-owned. Edit only AUTHOR sections.

## Component payload

<!-- COMPILER:BEGIN component-payload -->
```yaml
name: Maintenance job and part feature auditing
record_name: Contoso Service core-table auditing
satisfies:
- INTK-0001-REQ-007
- INTK-0001-REQ-011
- INTK-0001-REQ-013
scope:
- aks_maintenancejob:
  - aks_stage
  - aks_priority
  - aks_scheduleddate
  - aks_completeddate
  - aks_labourhours
  - aks_hourlyrate
  - aks_labourcost
  - aks_totalpartscost
  - aks_totalcost
- aks_jobpart:
  - aks_quantity
  - aks_unitprice
  - aks_linevalue
```
<!-- COMPILER:END component-payload -->

## Execution profile

<!-- COMPILER:BEGIN execution-profile -->
```yaml
evidence:
- privilege_matrix
- reviewer_evidence
executor_default: hybrid
source_sync_evidence:
- changed_solution_paths
target_roots:
- src/solutions
- config
validation_kinds:
- payload_conformance
- least_privilege_review
- import_smoke
```
<!-- COMPILER:END execution-profile -->

## Development resources

<!-- COMPILER:BEGIN development-resources -->
```yaml
capabilities:
  implementation_scope: repository_and_dataverse_environment
  matrix_hash: a34664afbe5b3f5adab7a380f584b38400573a33435b85b351282f3f8f37a6e7
  operations:
    create:
      executor:
        builder: unsupported
        supported: false
      fallback:
        allowed_when: not_applicable
        issue_evidence_required: false
        resource: power-platform-admin-center
      official_references:
      - audit_administration
      operation: any
      primary_resource: power-platform-admin-center
      profile: admin-center-primary
      rationale: Environment audit administration is not a custom-solution authoring
        operation.
      solution_context:
        mechanism: not_applicable
      support: unsupported_or_not_applicable
      verification:
        rationale: Verify only the configured audit scope after the administrator
          action.
        resource: dataverse-mcp
        tools:
        - describe
        - read_query
    delete:
      executor:
        builder: unsupported
        supported: false
      fallback:
        allowed_when: not_applicable
        issue_evidence_required: false
        resource: power-platform-admin-center
      official_references:
      - audit_administration
      operation: any
      primary_resource: power-platform-admin-center
      profile: admin-center-primary
      rationale: Environment audit administration is not a custom-solution authoring
        operation.
      solution_context:
        mechanism: not_applicable
      support: unsupported_or_not_applicable
      verification:
        rationale: Verify only the configured audit scope after the administrator
          action.
        resource: dataverse-mcp
        tools:
        - describe
        - read_query
    update:
      executor:
        builder: unsupported
        supported: false
      fallback:
        allowed_when: not_applicable
        issue_evidence_required: false
        resource: power-platform-admin-center
      official_references:
      - audit_administration
      operation: any
      primary_resource: power-platform-admin-center
      profile: admin-center-primary
      rationale: Environment audit administration is not a custom-solution authoring
        operation.
      solution_context:
        mechanism: not_applicable
      support: unsupported_or_not_applicable
      verification:
        rationale: Verify only the configured audit scope after the administrator
          action.
        resource: dataverse-mcp
        tools:
        - describe
        - read_query
    verify:
      executor:
        builder: unsupported
        supported: false
      fallback:
        allowed_when: not_applicable
        issue_evidence_required: false
        resource: power-platform-admin-center
      official_references:
      - audit_administration
      operation: any
      primary_resource: power-platform-admin-center
      profile: admin-center-primary
      rationale: Environment audit administration is not a custom-solution authoring
        operation.
      solution_context:
        mechanism: not_applicable
      support: unsupported_or_not_applicable
      verification:
        rationale: Verify only the configured audit scope after the administrator
          action.
        resource: dataverse-mcp
        tools:
        - describe
        - read_query
  policy:
    delete_semantics:
      delete: HTTP DELETE removes the metadata component or row from the environment;
        it is destructive and is never a substitute for removing solution membership.
      remove_solution_component: RemoveSolutionComponent removes only the component's
        membership from the named unmanaged solution; it does not delete the component
        from the environment.
    destructive_approval_required: true
    mcp_solution_authoring_rationale: Generic Dataverse MCP writes do not provide
      a compiler-validated explicit custom-solution context equivalent to the operation's
      documented Web API solution mechanism; Dataverse MCP is discovery and targeted
      post-action verification only for solution-aware authoring.
    portal_fallback_evidence_required: true
registry_hash: 4ce744923ad17777075eb9f015798d8be830327a349aa6a6ecfd2b78d6d80aee
required:
- authentication: none
  description: Official Microsoft implementation grounding.
  id: microsoft-learn
  kind: mcp
  purpose: audit_grounding
  rationale: Official audit guidance defines the supported administration path.
  server: microsoft-learn
  supported_execution_hosts:
  - local_interactive
  - github_copilot_cloud
  version: service-managed
  version_policy: service_managed
- authentication: interactive_user
  description: Human environment administration surface.
  endpoint: https://admin.powerplatform.microsoft.com
  id: power-platform-admin-center
  kind: portal
  purpose: primary_environment_audit_configuration
  rationale: The admin center is the registered environment-level audit authoring
    surface.
  supported_execution_hosts:
  - local_interactive
  version: service-managed
  version_policy: service_managed
- authentication: interactive_user
  description: GA Dataverse MCP endpoint; tool surface is probed at runtime.
  endpoint: https://org89912357.crm.dynamics.com/api/mcp
  id: dataverse-mcp
  kind: mcp
  purpose: targeted_audit_verification
  rationale: MCP is used only to verify the exact configured audit scope.
  release_channel: ga
  required_tools:
  - search
  - describe
  - read_query
  server: dataverse-authoring
  supported_execution_hosts:
  - local_interactive
  tools:
  - search_data
  - search
  - create_record
  - update_record
  - delete_record
  - create_table
  - update_table
  - delete_table
  - read_query
  - describe
  - upsert_skill
  - delete_skill
  - init_file_upload
  - commit_file_upload
  - file_download
  version: ga
  version_policy: service_managed
route_rationale: Dataverse audit enablement is an environment administration operation
  rather than custom-solution component authoring.
```
<!-- COMPILER:END development-resources -->

## Developer preflight

<!-- COMPILER:BEGIN developer-preflight -->
Required execution host: `local_interactive`.
Effective authentication policy: `reuse_if_valid`. Reuse a verified existing session; if it is absent, expired, mismatched, or under-permissioned, complete interactive authentication and revalidate.
`ready` means ready to attempt the exact scoped action. It does not prove that the live environment, solution, publisher, component, or permissions exist.
Broad discovery, solution export/unpack, absence scans, and cross-solution membership scans are not pre-action gates.

### 1. Availability and version

- **microsoft-learn.server-registration** — Verify the exact MCP server key is registered and reachable. (`required`, `automatic`, blocking)
  - Installation: `not_applicable` — Configure the official Microsoft Learn MCP endpoint in the active MCP client.
  - Server: `microsoft-learn`
  - Version: `service-managed`
  - Rationale: Official audit guidance defines the supported administration path.
- **power-platform-admin-center.portal-root** — Validate the official Power Platform admin center HTTPS root. (`required`, `automatic`, blocking)
  - Installation: `not_applicable` — Use a supported browser; no local package is installed.
  - Endpoint: `https://admin.powerplatform.microsoft.com`
  - Version: `service-managed`
  - Rationale: The admin center is the registered environment-level audit authoring surface.
- **dataverse-mcp.server-registration** — Verify the configured server key and resolved GA endpoint. (`required`, `automatic`, blocking)
  - Installation: `not_applicable` — Enable Dataverse MCP in the declared environment and register its GA endpoint in the active MCP client.
  - Server: `dataverse-authoring`
  - Endpoint: `https://org89912357.crm.dynamics.com/api/mcp`
  - Version: `ga`
  - Rationale: MCP is used only to verify the exact configured audit scope.

### 2. MCP and API capability

- **microsoft-learn.documentation-search** — Probe the server tool surface before implementation grounding. (`required`, `automatic`, blocking)
  - Server: `microsoft-learn`
  - Version: `service-managed`
  - Rationale: Official audit guidance defines the supported administration path.
- **dataverse-mcp.required-tools** — Verify every task-routed Dataverse MCP tool is exposed. (`required`, `automatic`, blocking)
  - Server: `dataverse-authoring`
  - Endpoint: `https://org89912357.crm.dynamics.com/api/mcp`
  - Version: `ga`
  - Required tools: `search`, `describe`, `read_query`
  - Rationale: MCP is used only to verify the exact configured audit scope.

### 3. Interactive authentication

- **power-platform-admin-center.administrator-sign-in** — Sign in as the current human with the required environment role. (`required`, `interactive`, blocking)
  - Existing-session verification: Confirm the current Power Platform admin center user, tenant, environment, and required role before reusing the browser session.
  - Fresh authentication: host/client cannot be forced programmatically.
  - Fresh-auth action: Sign out only the current admin-center account or open a new private browser session, sign in interactively, and select the declared environment before resuming.
  - Endpoint: `https://admin.powerplatform.microsoft.com`
  - Human action: Complete browser sign-in and verify the declared environment before continuing.
  - Rationale: The admin center is the registered environment-level audit authoring surface.
- **dataverse-mcp.interactive-sign-in** — Authenticate the current human directly to the declared Dataverse environment. (`required`, `interactive`, blocking)
  - Existing-session verification: In the active MCP host, inspect the dataverse-authoring connection and confirm the signed-in user and declared environment without reconnecting.
  - Fresh authentication: host/client cannot be forced programmatically.
  - Fresh-auth action: In the active MCP host, disconnect or sign out only the dataverse-authoring connection, reconnect the declared /api/mcp endpoint, and complete browser or device sign-in before resuming.
  - Endpoint: `https://org89912357.crm.dynamics.com/api/mcp`
  - Human action: Complete the MCP browser or device sign-in and return only non-secret identity results.
  - Rationale: MCP is used only to verify the exact configured audit scope.

### 4. Authenticated endpoint session

- **authentication.endpoint-session** — Confirm an authenticated, callable session exists for an exact compiler-routed endpoint without broad environment, solution, publisher, component, membership, or permission discovery. (`required`, `automatic`, blocking)
  - Accepted authenticated endpoints: `https://admin.powerplatform.microsoft.com`, `https://org89912357.crm.dynamics.com/api/mcp`
  - Authentication resources: `dataverse-mcp`, `power-platform-admin-center`

### 5. Compiler-owned target configuration

- **target.compiler-configuration** — Require concrete compiler-owned environment, routed target, canonical identity, and interactive authentication configuration. Do not live-enumerate or preverify their existence. (`required`, `automatic`, blocking)
  - Environment URL: `https://org89912357.crm.dynamics.com`
  - Environment ID: `89a7081d-6701-f111-8c79-6045bd005919`
  - Component identity: `Contoso Service core-table auditing`

### 6. Ready to attempt

- **ready-to-attempt.required-safety** — Permit the first scoped platform action only after host/tool, live Project and compiler integrity, resource/version, endpoint-session, and structural target checks pass. (`required`, `automatic`, blocking)

### Direct scoped action

- Invoke only the DEV/build-skill action against the exact compiler-owned endpoint and target; never fall back to another environment or solution.
- Do not export or unpack a solution, enumerate solution/publisher inventories, scan component absence, or inspect cross-solution membership before the first action.
- Surface sanitized platform/MCP/PAC errors and stop without further writes; never create or repair environment, publisher, or solution prerequisites.
- Route rationale: Dataverse audit enablement is an environment administration operation rather than custom-solution component authoring.
- The operation primary is selected from the compiler-owned capability matrix; Maker Portal cannot be selected for convenience.
- Portal fallback requires sanitized issue evidence naming the exact API failure or unsupported capability.
- Environment URL: `https://org89912357.crm.dynamics.com`
- Canonical identity: `record_name=Contoso Service core-table auditing`
- Resource action **power-platform-admin-center.administrator-action** — Perform only the explicit environment administration checkpoint.

#### Capability routes

- `create` → `power-platform-admin-center` (`unsupported_or_not_applicable`; no Web API request); Environment audit administration is not a custom-solution authoring operation.
- `delete` → `power-platform-admin-center` (`unsupported_or_not_applicable`; no Web API request); Environment audit administration is not a custom-solution authoring operation.
- `update` → `power-platform-admin-center` (`unsupported_or_not_applicable`; no Web API request); Environment audit administration is not a custom-solution authoring operation.
- `verify` → `power-platform-admin-center` (`unsupported_or_not_applicable`; no Web API request); Environment audit administration is not a custom-solution authoring operation.

### Post-action verification

- After a successful write, target-read the exact canonical identity and verify the expected DEV payload outcomes.
- Record sanitized non-secret evidence. Full solution export/unpack is not a verification mechanism.
Registry SHA-256: `4ce744923ad17777075eb9f015798d8be830327a349aa6a6ecfd2b78d6d80aee`. Capability matrix SHA-256: `a34664afbe5b3f5adab7a380f584b38400573a33435b85b351282f3f8f37a6e7`. This compiler-owned snapshot contains no credentials.
<!-- COMPILER:END developer-preflight -->

## Implementation Notes

<!-- AUTHOR:BEGIN implementation-notes -->
- Extend existing environment auditing (`record_name: Contoso Service core-table auditing`, established in FEAT-01 / DEV-0015) to the new feature columns. This is an **environment-bound** configuration with **no custom-solution membership** — do not add it to `ContosoServiceCore` or any solution.
- Enable **column-level** auditing for the DEV-0016 `aks_maintenancejob` columns (`aks_stage`, `aks_priority`, `aks_scheduleddate`, `aks_completeddate`, `aks_labourhours`, `aks_hourlyrate`, `aks_labourcost`, `aks_totalpartscost`, `aks_totalcost`) and the DEV-0017 `aks_jobpart` columns (`aks_quantity`, `aks_unitprice`, `aks_linevalue`).
- Auditing captures only when all three levels are ON: organization-level, table-level (on `aks_maintenancejob`/`aks_jobpart`), and per-column. Org and table auditing were enabled in FEAT-01 (DEV-0015); this DEV adds the new columns only — confirm the two upper levels remain enabled so column capture is effective.
- **Depends on DEV-0016 and DEV-0017** — the columns must exist before their audit flag can be set. Audit only the compliance-required financial/status columns above (least privilege), nothing more. Preview then verify the effective audit settings.
- Traceability: INTK-0001-REQ-007, INTK-0001-REQ-011, INTK-0001-REQ-013.
<!-- AUTHOR:END implementation-notes -->

## Checkpoints

<!-- AUTHOR:BEGIN checkpoints -->
- Ordering: DEV-0016 and DEV-0017 columns must exist first; block if any scoped column is absent.
- Environment-bound gate (hybrid): this changes environment audit configuration, not a solution — confirm no solution membership is created and that org + table auditing remain enabled so column capture is effective.
<!-- AUTHOR:END checkpoints -->

## Evidence

<!-- AUTHOR:BEGIN evidence -->
- `changed_solution_paths`: environment-bound — no `ContosoServiceCore` change; capture the audit-configuration artifact under `config/` (or the effective environment audit settings) instead.
- `privilege_matrix`: the audited-columns matrix per table showing exactly the scoped compliance columns enabled (and nothing beyond), satisfying least_privilege_review.
- `reviewer_evidence`: named reviewer confirming organization + table + column auditing all enabled and environment-bound (no solution membership); sanitized before/after settings posted via `scripts/execution_evidence.py`.
<!-- AUTHOR:END evidence -->
